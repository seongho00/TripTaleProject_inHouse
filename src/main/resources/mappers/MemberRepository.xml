<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.MemberRepository">

	<select id="doJoin" parameterType="map">

		INSERT INTO `member`
		SET
		regDate = NOW(),
		updateDate = NOW(),
		provider = #{provider},
		providerId =
		#{providerId},
		loginPw = #{loginPw},
		`name` =
		#{name},
		email = #{email},
		profileImage = #{profileImage}

	</select>


	<select id="getMemberById" parameterType="map">
		SELECT M.*, MI.data AS
		extra__data
		FROM `member` AS M
		INNER JOIN memberImage AS MI
		ON M.id =
		MI.memberId
		WHERE M.provider = #{provider} AND M.providerId =
		#{providerId};
	</select>

	<select id="getLastInsertId" parameterType="int">
		SELECT
		LAST_INSERT_ID()
	</select>

	<select id="getMemberByLoingId" parameterType="map">
		SELECT M.*,
		MI.data
		AS
		extra__data
		FROM
		`member`
		INNER JOIN memberImage AS MI
		ON M.id =
		MI.memberId
		WHERE M.loginId = #{loginId}
	</select>


	<select id="getMemberImageIdByMemberId" parameterType="map">
		SELECT *
		FROM
		memberImage
		WHERE memberId = #{memberId}
	</select>


	<select id="updateMemberImage" parameterType="map">
		UPDATE
		memberImage
		SET fileName = #{fileName},
		data =
		#{imageBytes}
		WHERE memberId =
		#{memberId}
	</select>

	<select id="getMemberImageByMemberId" parameterType="map"
		resultType="com.example.demo.vo.MemberImage">
		SELECT *
		FROM memberImage
		WHERE memberId = #{memberId}
	</select>

	<select id="insertMemberImage" parameterType="map"
		resultType="com.example.demo.vo.MemberImage">
		INSERT INTO memberImage
		SET memberId = #{memberId},
		fileName
		= #{fileName},
		data = #{imageBytes}
	</select>


	<select id="doDelete" parameterType="map"
		resultType="com.example.demo.vo.MemberImage">
		DELETE FROM member
		WHERE id = #{memberId}
	</select>

	<select id="getMemberByNameAndEmail" parameterType="map"
		resultType="com.example.demo.vo.Member">
		SELECT *
		FROM member
		WHERE provider = 'local' AND name =
		#{name} AND email = #{email}
	</select>


	<select id="getMemberByProviderId" parameterType="map"
		resultType="com.example.demo.vo.Member">
		SELECT *
		FROM member
		WHERE provider = 'local' AND providerId
		= #{providerId}
	</select>


	<select id="updateMember" parameterType="map">
		UPDATE
		member
		<set>
			<if test="loginPw != ''">
				loginPw = #{loginPw},
			</if>
			<if test="name != ''">
				name = #{name},
			</if>
			<if test="email != ''">
				email = #{email},
			</if>
			updateDate = NOW()
		</set>
		WHERE id = #{memberId}
	</select>

	<select id="getMemberByMemberId" parameterType="map"
		resultType="com.example.demo.vo.Member">
		SELECT *
		FROM member
		WHERE id = #{memberId}
	</select>



</mapper>